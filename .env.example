# Optional: Free LLM (Groq) for "First measures" suggestions on Results page.
# Get a free API key at https://console.groq.com — then copy this file to .env and set:
# VITE_GROQ_API_KEY=your_groq_api_key_here
# If not set, rule-based fallback tips are shown instead.
